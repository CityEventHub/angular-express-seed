
exports.load = function(crud, gm) {
	// app.get('/api/users', crud.getCollection(User));
	// app.post('/api/users', gm.permissions, crud.postCollection(User));

	// app.get('/api/users/:_id', crud.getDocument(User));
	// app.put('/api/users/:_id', gm.permissions, crud.putDocument(User));
	// app.delete('/api/users/:_id', gm.permissions, crud.deleteDocument(User));
}
