
exports.load = function(crud, gm) {
	// app.get('/api/names', crud.getCollection(Name));
	// app.post('/api/names', gm.permissions, crud.postCollection(Name));
	// // these are rare. Normally we don't use put or delete on a collection.
	// app.put('/api/names', gm.permissions, crud.putCollection(Name));
	// app.delete('/api/names', gm.permissions, crud.deleteCollection(Name));

	// // Name Documents:
	// app.get('/api/names/:_id', crud.getDocument(Name));
	// app.put('/api/names/:_id', gm.permissions, crud.putDocument(Name));
	// app.delete('/api/names/:_id', gm.permissions, crud.deleteDocument(Name));
}
